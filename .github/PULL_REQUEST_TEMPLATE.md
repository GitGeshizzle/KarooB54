# Summary

<!-- What does this change and why? -->

## Checklist
- [ ] `./gradlew :app:test` passes
- [ ] Builds (`./gradlew :app:assembleDebug`)
- [ ] Tested on a Karoo (if it affects runtime behaviour) — note your light/firmware & Karoo
- [ ] Communication with the light stays read-only (no state-changing commands added)
